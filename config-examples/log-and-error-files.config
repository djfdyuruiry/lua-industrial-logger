config {
    appenders {
        file "log-file" {
            logFilePath = "/tmp/app.log"
        },
        file "error-file" {
            level = ERROR,
            logFilePath = "/tmp/app_err.log"
        }
    }
}
