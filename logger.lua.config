config {
    pattern "%{iso8601}d [%t] %l %n - %m",
    appenders {
        console "out" {
            stream = "stdout"
        },
        console "error" {
            stream = "stderr"
        }
    }
}
